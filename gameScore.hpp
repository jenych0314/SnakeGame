#ifndef GAMESCORE_HPP
#define GAMESCORE_HPP

#include <chrono>

typedef struct Score_menu
    {
        int max_len;
        int cur_len;
        int apple_score;
        int poison_score;
    } s_score;


#endif